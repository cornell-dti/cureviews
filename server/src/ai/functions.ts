// import { Classes } from "../../db/schema"; // <- Want to access the database? 

/** Docstring for simpleCosting. It returns the same number you passed in. 
 * @params num (number) -> 
 * @returns number 
 */
async function simpleCosting(num: number) {
  return num
}

export { simpleCosting }



